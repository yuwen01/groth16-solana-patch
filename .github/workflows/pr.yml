name: PR

on:
  push:
    branches: [master]
  pull_request_target:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a test
        run: |
          echo "This workflow was AGAIN triggered by a ${{ github.event_name }} event."
          echo "Branch: ${{ github.ref }}"